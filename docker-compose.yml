services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb+srv://admin:Hoangbinh059@profile.l4i3mtb.mongodb.net/profiledb?retryWrites=true&w=majority
      - SPRING_DATA_MONGODB_DATABASE=profiledb
    restart: unless-stopped
